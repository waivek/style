1. Make website using modern features, such as CSS-colors, clickable divs
instead of buttons and then work separately on accessibility by using feature
detection to provide fallbacks such as hardcoded colors or replacing the
clickable divs with buttons.
